<h1> Edit a job </h1>

<%= simple_format [:admin, @job] do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :wage_lower_bound, :label => "Wage-Lower" %>
<%= f.input :wage_upper_bound, :label => "Wage-Upper" %>
<%= f.input :contact_email %>

  <%= f.submit "Submit" %>
<% end %>
